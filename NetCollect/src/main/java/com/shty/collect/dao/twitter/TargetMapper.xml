<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shty.collect.dao.twitter.TargetMapper">
    <select id="findAll" resultType="Target">
        SELECT *
        FROM t_target t
        WHERE t.Valid = 1
    </select>
</mapper>